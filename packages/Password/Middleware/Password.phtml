<?php
/** @var ObjectivePHP\Middleware\Action\PhtmlAction\PhtmlAction $this */
?>

<div class="modal" tabindex="-1" role="dialog" style="display: block; position: relative">
    <div class="modal-dialog" role="document">
        <form class="modal-content" method="post">
            <div class="modal-header">
                <h5 class="modal-title">Dites le mot de passe</h5>
            </div>
            <div class="modal-body">
                <?php if (!empty($this->get('message'))) : ?>
                    <div class="alert alert-danger">
                        <?= htmlspecialchars($this->get('message'), ENT_QUOTES|ENT_SUBSTITUTE) ?>
                    </div>
                <?php endif; ?>

                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <input type="password" name="password" id="password" class="form-control">
                    <small class="form-text text-muted">N'hésitez pas à nous demander le mot de passe :-)</small>
                </div>
            </div>
            <div class="modal-footer">
                <input type="submit" class="btn btn-primary" value="Valider">
            </div>
        </form>
    </div>
</div>
